# Development & testing dependencies (not needed for end users)
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=7.4.0
pytest-mock>=3.12.0
openpyxl>=3.1.0      # needed by conftest.py sample_xlsx fixture

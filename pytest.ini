[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: pure unit tests (no I/O)
    integration: tests that use real DB or temp files
    slow: tests that call real external APIs (skipped by default)
